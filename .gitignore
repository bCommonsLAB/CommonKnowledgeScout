# dependencies
node_modules
.pnpm-store/
.pnp
.pnp.js

# testing
# Nur das Test-Coverage-Output im Wurzelverzeichnis — ein unverankertes
# `coverage` hat die API-Route `.../agent-view/coverage/route.ts` verschluckt
# (deshalb fehlte die GET-Route aus Welle 1 im Repo).
/coverage/
test-results/
playwright-report/
playwright/.cache/

# lokale Arbeitsdateien (E2E-Login-Session, Test-Quelldaten — nie committen)
tmp/

# next.js
.next/
out/
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*.local
.env


# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts 

.obsidian

# samples
template-samples/diva sample content/*
# Original-Website-Dump (Webflow-HTML + Fremd-Libs + grosse Bilder) — nur lokale Referenz
template-samples/oldiesforfuture-original/

# Reparatur-/Migrations-Backups (enthalten Prod-Daten, nie committen)
backups/
