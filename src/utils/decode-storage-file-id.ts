/**
 * Liegt seit der Welle „Galerie-Langer-Schwanz" in `@ks/util` — eine reine
 * Funktion ohne Umgebungsbezug. Hier weitergereicht, damit bestehende
 * Importpfade bleiben.
 */
export { tryDecodeRelativePathFromFileId } from '@ks/util'
