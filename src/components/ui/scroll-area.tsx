// Fassade (G2): ScrollArea-Primitives leben jetzt in @ks/ui (shadcn-Basis).
// Breite Importeure (`@/components/ui/scroll-area`) bleiben so unveraendert
// lauffaehig; eine Folgewelle kann sie nach und nach auf `@ks/ui` umstellen.
export { ScrollArea, ScrollBar } from "@ks/ui"
